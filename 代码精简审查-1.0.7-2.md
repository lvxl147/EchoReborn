# EchoReborn 代码精简审查报告

审查基线：**1.0.7-2**（commit `9692a966`）
审查日期：2026-09-17
本轮**未修改任何代码**，仅输出清单与风险分析。

---

## 0. 备份与回退点（执行前已完成）

| 项目 | 内容 |
|---|---|
| 全量源码备份 | `C:\Users\Administrator\WorkBuddy\ios插件开发\_backup_EchoReborn_20260917-0630\EchoReborn` |
| 备份校验 | 168 个文件 / 3,600,708 字节 —— 与源目录**逐文件一致** |
| 二进制回退点 | 1.0.7-2 的 rootless / roothide deb（已发布，可随时降级重装） |
| 回退方式 | 直接用备份目录覆盖源目录即可；无需重装即可恢复到本报告对应状态 |

---

## 1. 审查方法与一个决定性前提

用 7 轮脚本化扫描取证（脚本留在 `C:\Users\Administrator\_audit*.py`，可复跑）：

1. 剥离注释后再统计引用 —— 否则「只在注释里出现」的符号会被误判为已使用；
2. 按 C 的**引号包含语义**解析 `#import "x.h"`（先相对 includer 所在目录），而不是简单按文件名匹配 —— 这直接决定了三组同名头文件是真在用还是孤立副本；
3. 展开 Makefile 的 `$(wildcard ...)` 后再判断「哪些源文件真的被编译」；
4. 解开 deb 载荷，核对**实际入包**的 plist 与 dylib。

> **决定性前提（Makefile 第 77–78 行）**
> ```
> EchoReborn_CFLAGS = -fobjc-arc -Wno-nullability-completeness -Wno-deprecated-declarations \
>                     -Wno-unused-variable -Wno-unused-function
> ```
> 项目显式关掉了 `-Wunused-variable` / `-Wunused-function`。**这是本机能积累死代码的结构性原因**：定义了但从未被引用的静态函数/常量不会产生任何编译告警，会安静地编进二进制。

---

## 2. 可安全删除清单

### A1 · 孤立文件（6 个，共 292 行）—— 零引用，删除不影响编译

| # | 文件 | 行数 | 删除理由 | 潜在风险 |
|---|---|---|---|---|
| A1-1 | `LiquidSiri/Shared/LGCoverSheetState.h` | 114 | 按引号包含语义解析后**没有任何编译单元会解析到它**（GlassKit 的 .m 包含的是同名同目录副本） | 极低。唯一需注意：它与 GlassKit 版本**内容不同**（状态文件路径不同），若将来有人"顺手"包含它会引入漂移 —— 这恰恰是删除它的收益 |
| A1-2 | `LiquidSiri/Shared/LGLiveBackdropView.h` | 38 | 同上；且它只是 GlassKit 版本的**子集**（缺 `lgShapeRect` / `lgShapeCornerRadius` / `lgInvalidateFilterContents` / `lgFilterAttached` / `lgBackdropZoom`） | 极低。这份子集声明本就不该存在：同一个类在一个进程里看到两份不同声明是隐患，删除等于消除隐患 |
| A1-3 | `LiquidSiri/Shared/LGPrefAccessors.h` | 12 | 无任何引用 | 极低 |
| A1-4 | `LiquidSiri/Shared/LGLiquidMotion.h` | 89 | 无任何引用；其内部 4 个 `static inline` 函数同样零引用 | 极低 |
| A1-5 | `LiquidSiri/Shared/LGRWBSupport.h` | 3 | 唯一包含者是 A1-6，而 A1-6 不参与编译 | 极低 |
| A1-6 | `LiquidSiri/Shared/LGRWBSupport.m` | 36 | **未被 Makefile 编译**：`EchoReborn_FILES` 逐个列出了 LiquidSiri/Shared 下的 .m，其中没有它；`LiquidSiri/Shared/*.m` 也不存在通配符 | 低。断言依据是「展开通配符后的编译集合」，已复核 Makefile 第 48–72 行 |

> 合计：**292 行孤立代码**，其中 6 个文件里 5 个是头文件（本就不占用二进制体积），唯一的 .m 从未被编译 —— 即**对最终 deb 零影响**。

### A2 · 重复文件（1 个，56 字节）

| # | 文件 | 删除理由 | 潜在风险 |
|---|---|---|---|
| A2-1 | `BackboarddGlass/EchoRebornBackboardd.plist` | 与项目根的 `EchoRebornBackboardd.plist` **md5 完全相同**（`56cf829766`）。Theos 按 `$(TWEAK_NAME).plist` 约定取**项目根目录**的那份；deb 载荷核对已证实入包的是根目录版本（`…/DynamicLibraries/EchoRebornBackboardd.plist`） | 极低。唯一前提是「Theos 只认根目录」，已用 1.0.7-2 的 deb 载荷反向验证过 |

### A3 · 未被任何代码引用的静态符号（11 个）

在 `-Wno-unused-function` 下它们不会告警，会实际编进二进制。删除后调用点数为 0，无路径可断。

| # | 符号 | 位置 | 删除理由 | 潜在风险 |
|---|---|---|---|---|
| A3-1 | `ERShortcutIconImage` | `Tweak.xm:3237` | 1.0.6-13（第 5 条）把「添加控制项」预览换成无背景字形图后，它失去最后一个调用点（现存提及只出现在 README/VERSIONING 等文档里） | 低。是**我改动造成的死代码**，删它属于清理自己的遗留 |
| A3-2 | `ERShortcutPlaceholderIcon` | `Tweak.xm:3274` | 已被 `ERShortcutPlaceholderIconForStyle(NO)` 取代（磁贴要无背景版），仅剩文档提及 | 低。注意**不要**连带删 `ERShortcutPlaceholderIconForStyle` —— 它仍在用 |
| A3-3 | `gERShortcutProbedPaths` | `Tweak.xm:2889` | 静态常量，零引用（仅文档提及） | 极低 |
| A3-4 | `kERSystemReload` | `prefs/ERSystemEnhanceController.m:12` | 零引用 —— plist 里通知名是硬编码字面量，这个常量从未被取用 | 极低。二选一：删除，或反过来让代码引用它（我倾向删除，保持 plist 为唯一真值源） |
| A3-5 | `liquidGlassShutdown` | `BackboarddGlass/Tweak.mm:428` | 零引用的静态函数 | 低。该文件属 `EchoRebornBackboardd` 目标，删除只影响该 dylib 的局部符号 |
| A3-6 | `generateWavePaths` | `LiquidSiri/Tweak.x:157` | 零引用 | 低。同为 TU 内 `static`，不会被 Swift 侧引用（statics 不出符号） |
| A3-7 | `notifyToken` | `LiquidSiri/Tweak.x:783` | 零引用的静态常量 | 极低 |
| A3-8 | `LGSpecularMotionInit` | `LiquidSiri/Runtime/LGLiquidGlassRuntime.m:279` | 零引用 | 低 |
| A3-9 | `LGAppendLogLine` | `LiquidSiri/Shared/LGSharedSupport.m:134` | 零引用 | 低。该文件仍在编译（含其他在用符号），只删这一个函数 |
| A3-10 | `LGLensRectWrite` | `GlassKit/LGLensRectState.h:67` | 零引用；该头文件**只被 `BackboarddGlass/Tweak.mm` 一个 TU 包含**（经 `../GlassKit/LGLensRectState.h` 路径），所以删除不波及别处 | 低 |

> 说明：**不做方法级删除**。Objective-C 是动态派发，方法是否被调用在静态期无法判定（`respondsToSelector:` / `performSelector:` / Logos 的 `%hook` 都可能触达），任何「看起来没人调」的方法都不能据此判死。这是本次审查刻意划下的边界，也是「行为完全不变」这条硬约束的前提。

### A4 · 看起来像冗余、但**不能删**的项（避免误删）

| 项 | 为什么不能删 |
|---|---|
| `prefs/Resources/EchoRebornPrefs@2x.png` / `@3x.png` | 代码里永远不会出现这两个文件名 —— iOS 按 `@2x/@3x` 约定自动解析（`EchoRebornPrefs.plist` 的 `icon` 写的是 `EchoRebornPrefs.png`）。删掉后图标在高分屏上会变糊。**这是本清单里最容易误删的一项** |
| `prefs/Resources/{Siri,Soko,SystemEnhance}.plist` | 通过 `loadSpecifiersFromPlistName:@"Siri"` 加载（不带扩展名），按文件名搜会误判为未使用。已核对入包载荷：三份都在 |
| `EchoRebornBackboardd.plist`（根目录） | Theos 按 `TWEAK_NAME` 约定取根目录；与 A2-1 只差位置 |
| `assets/banner.png` | `depiction.json` 实际引用 1 次（Sileo 详情页头图） |
| `assets/icon.png` | 供 Sileo 详情页/仓库展示，非代码引用（与 `prefs/Resources/icon.png` 是两张不同的图） |
| 大量中文注释 | **不属于冗余**。它们记录了 0.5.x 以来的踩坑结论（例如 `cellClass` 必须传类对象、GlassKit 强制隐藏材质），是本项目的主要知识载体，删除才是真的损失 |

### A5 · 跨二进制重复常量（本轮不动，建议单独一轮）

`kERLogDirectory` / `kERLogFileName` / `kERLogExportedPrefix` / `kERLogExportedDirectory` / `kERShortcutSnapshotFileName` / `kERShortcutPrefsDomain` 等，在 **EchoReborn.dylib 与 EchoRebornPrefs.bundle 两个二进制里各有一份**。

- 性质：不是 bug，是「两个二进制必须各自持有」的物理事实；
- 但它们是**手工保持同步的重复**（代码注释里写着 "Must match kERPrefsDomain in Tweak.xm"）；
- 简化方案是抽一个共享头 `ERSharedConstants.h`，两侧 `#import`。**收益中等，风险中等**（改动会触及日志路径与快捷指令快照路径，一旦写错就是"日志丢/快照读不到"这类静默故障），因此我建议**与本次精简分开**，单独一轮做并单独验证。

---

## 3. 高风险项：发现了一个「不能简单删」的真实隐患（重要）

### H1 · 同一个 dylib 里存在两份**内容不同**的 `LGHostRegistry.h`

| 位置 | 谁真正包含它 |
|---|---|
| `GlassKit/LGHostRegistry.h`（93 行） | `GlassKit/LGGlassKit.x`、`GlassKit/LGLiveBackdropView.m` |
| `LiquidSiri/Shared/LGHostRegistry.h`（90 行） | `LiquidSiri/Tweak.x` |

两份**都被编译进 `EchoReborn.dylib`**，但内容不同：

- 类成员不同：`float bezelWidthPoints;` vs `float bezelRatio;`
- 注册表条目**数量与参数都不同**：GlassKit 版是 `echoreborn.liquidglass.*` 前缀、参数列为 10 个数值 + 两个颜色；LiquidSiri 版是 `dylv.liquidglass.*` 前缀、参数列结构不同（例如 `Banner` 一行的第 5 个参数从 `20.75f` 变成 `0.25f`，`Default` 从 `20.00f` 变成 `(28.0f / 220.0f) * 1.8f`）

**为什么危险**：这正是 Makefile 第 39–47 行注释描述过、并在 0.5.25 上"把设备搞成 respring 循环"的同一类隐患 —— 同一进程里同名类型/表的多份定义。当前构建能通过，是因为它们只作为 `static` 表与 `static inline` 函数出现在各自的 TU 里，没触发链接期重复符号；但**运行期的玻璃参数取决于哪个调用点落在哪个 TU**，这属于"看起来是冗余、实则不能直接删"的典型。

**处置建议（不并入本轮）**：单独一轮做「合并为单一头」，逐项人工确认那 30 余处参数差异是**刻意分流**还是**上游漂移**；在没弄清之前，任何一侧的删除都可能让某条玻璃路径的参数整表换掉 —— 那会直接改变视觉效果，违反本次"界面完全不变"的前提。

### H2 · `LGCoverSheetState.h` 两份只差一个文件路径

GlassKit 版写 `/var/mobile/Library/Accessibility/echoreborn-liquidglass-coversheet-state.bin`，LiquidSiri 版写 `…/liquidglass-coversheet-state.bin`。
LiquidSiri 那份已被列入 A1-1 删除（当前零引用）；**保留的 GlassKit 版才是生效路径**。删除前我已确认 `LiquidSiri/Tweak.x` 未通过宏间接使用它。

---

## 4. 「删完不会出问题」的论证

删除 A1 + A2 + A3 之后：

1. **编译单元集合不变** —— A1 全部不被任何 TU 解析到；A1-6 本就不在 Makefile 的编译列表里（已展开通配符复核）。因此不会出现「删了头文件导致某个 .m 编译失败」或「删了 .m 导致符号缺失」。
2. **可执行符号集合只减不增** —— A3 的 11 个符号全项目调用点数为 0（剥离注释后统计），删除不会切断任何调用路径。
3. **打包载荷不变** —— A2-1 从未入包（deb 载荷已核对）。
4. **不触碰任何绘制/布局/资源路径** —— A1–A3 不含任何 UIView/图层/图片/plist 数据，因此**渲染结果逐像素不变**是结构性结论，而非经验判断。

**可验证的验收标准（建议照此执行）**

| 验收项 | 期望结果 |
|---|---|
| deb 载荷条目数 | 删除 A1+A2 后**完全不变**（54 条 / rootless） |
| `EchoReborn.dylib` 符号表（`nm -a`） | 差异**只包含 A3 那 11 个局部符号的消失**，不出现新增符号 |
| 界面 | 设置页各页、控制中心各磁贴、锁屏/Siri 全部目视一致 |
| 功能 | 七个连接磁贴、天气、快捷指令、模块管理、翻页、日志导出逐项复测 |

---

## 5. 建议的执行顺序

1. **第 1 步**：删 A1（6 个孤立文件）+ A2（1 个重复 plist）→ 构建 → 比对 deb 载荷条目（应完全不变）→ 发 1.0.7-3
2. **第 2 步**：删 A3（11 个未引用静态符号）→ 构建 → 比对 dylib 符号表（应只减不增）→ 发 1.0.7-4
3. **第 3 步（可选）**：A5 跨二进制常量抽公共头 → 单独一轮
4. **H1 高风险项**：单独立项，先做差异归类，再决定合并方式

每一步都保留独立版本号，便于你逐版回退定位。本次精简的总规模约 **292 行孤立代码 + 11 个死符号 + 1 个重复文件**；不夸大地说 —— 这个项目**不是一个冗余严重**的项目，主要体积来自大段有价值的注释与两套上游移植代码，**可安全删减的空间有限**，我没有为了"交差"而把有风险的东西列进来。

---

## 附：审查脚本（可复跑）

| 脚本 | 作用 |
|---|---|
| `_audit1.py` | 首轮引用计数（未剥离注释） |
| `_audit2.py` | 剥离注释后严格计数 + `%group/%init` 一致性 + 空 `%hook` 体 |
| `_audit3.py` | 同名文件 md5 比对 + 展开通配符后的「未编译源码」 |
| `_audit4.py` | 同名头文件剥注释后语义 diff + 资源引用核验 |
| `_audit5.py` | 头文件性质（static 表/内联）+ 包含关系 + depiction 引用 |
| `_audit6.py` | 全项目零引用头文件扫描 |
| `_audit7.py` | 按引号包含语义做**真实**包含关系核验（决定性的一轮） |
| `_audit8.py` | 解开 deb 载荷，核对实际入包的 plist/dylib |

---

## 补充测量（1.0.7-3 发布后）：**本报告的一处结论需要更正**

有人在精简后提出问题：「为什么安装包大小没变？」为此做了实测，结论如下 ——
**报告第 1 节把 `-Wno-unused-function` 说成「死代码会编进二进制」，这句话是错的。**

### 实测数据（1.0.7-2 → 1.0.7-3）

| 指标 | 1.0.7-2 | 1.0.7-3 | 差 |
|---|---|---|---|
| deb 文件（rootless） | 1,019,684 B | 1,018,676 B | −1,008 B |
| deb 文件（roothide） | 1,014,310 B | 1,018,574 B | +4,264 B |
| 载荷解压后总字节 | 4,911,824 B | 4,911,824 B | **0** |
| `EchoReborn.dylib` | 2,535,472 B | 2,535,472 B | **0** |
| `EchoRebornBackboardd.dylib` | 306,192 B | 306,192 B | **0** |

Mach-O 段/节级别（arm64e 切片）：

| 段 / 节 | 1.0.7-2 | 1.0.7-3 | 差 |
|---|---|---|---|
| `__TEXT` 段 | 1,015,808 | 1,015,808 | **0** |
| `__text` 节 | 732,792 | 732,792 | **0** |
| `__const` / `__cstring` | 14,816 / 61,713 | 同 | **0** |
| `__unwind_info` | 11,040 | 11,040 | **0** |

### 决定性佐证

删掉的 `LGAppendLogLine` 及 LG 日志子系统里，有几个**只属于它们**的字符串字面量。
在两版 dylib 中逐一检索：

| 字符串 | 在 1.0.7-2 中出现 | 在 1.0.7-3 中出现 |
|---|---|---|
| `[LiquidAss] log file create failed %@` | **0** | 0 |
| `[LiquidAss] log file append failed %@` | **0** | 0 |
| `dylv.liquidass.logfile` | **0** | 0 |
| `NSFileHandle exception` | **0** | 0 |
| `/tmp/liquidglass.log` | **0** | 0 |

**这些字符串在 1.0.7-2 里就已经不存在了** —— 说明那些函数**早已被编译器/链接器从二进制中剔除**，
只是源码还留着。所以把它们从源码里删掉，二进制一个字节都不会变。

### 结论（更正）

1. `-Wno-unused-variable` / `-Wno-unused-function` **只关闭告警，不会让死代码进入二进制**。
   真正的剔除发生在优化与链接阶段（未引用的内部函数被 DCE / `-dead_strip` 移除）。
   本报告第 1 节把它描述为「会安静地编进二进制」属于**推断错误**，特此更正。
2. 因此本次精简**不可能**减小安装包 —— 可删的东西在二进制里根本不存在。
   deb 那 ±KB 的浮动只是压缩差异（载荷内容除版本串与 `LC_UUID` 外完全一致）。
3. 精简的真实收益是**源码可信度**，不是体积：清掉了会误导人的孤立文件，
   并消除了两份**内容不同的同名头**（`LGHostRegistry.h` / `LGCoverSheetState.h` / `LGLiveBackdropView.h` 的 LiquidSiri 副本）——
   它们一旦被误包含，会让同一个 dylib 里出现两套玻璃参数，是比体积严重得多的问题。
4. 若真要减体积，唯一的杠杆是**动活代码**（不是死代码）：
   - 7 个连接模块 bundle 各自内嵌一份模块实现，合计约 1.17 MB（未压缩），占载荷 23.7%；
   - `ERWeatherModule` bundle 239 KB；
   - 换 `-Os` 尺寸优化、裁 `__unwind_info` 等 —— 每项都只省几十 KB 量级。
   一个含 8 个 bundle + 2 个 dylib 的控制中心套件，1 MB 左右属正常规模。

