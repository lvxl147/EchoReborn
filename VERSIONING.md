# Echo Reborn 版本号规则

用户约定（2026-09-10 确立）：

- 当前版本线从 **0.3.0** 起算。
- **每完成 10 次迭代（构建/发布），次版本号 +1**：0.3.0 → 0.4.0 → 0.5.0 ……
- 未满 10 次迭代时，同一版本号下只递增 `CFBundleVersion`（prefs/Resources/Info.plist）。
- 修订号（第三位）保持 0，不用于日常迭代计数。

## 迭代计数

| 迭代 | 版本 | 说明 |
| --- | --- | --- |
| 1 | 0.3.0 | 修复计时器黑底、滑块实时读数、分类管理空白、状态栏偶发消失 |
| 2 | 0.3.1 | 分类管理空白页根治：Root.plist 恢复 bundle 键、控制器修正加载顺序 |
| 3 | 0.3.2 | 分类管理改为 ERRootListController 直接 push；滑块改用自装手势 + 运行时类枚举；计时器磁贴显式灰色底 |
| 4 | 0.3.3 | 修复 0.3.2 注销死循环：运行时 swizzle 彻底移除 class_replaceMethod 路径，仅保留 class_addMethod，结构上不可能递归 |
| 5 | 0.3.4 | 修复 0.3.3 仍卡注销：彻底删除 -layoutSubviews 运行时 swizzle 与调用期继承链遍历（被 liquidass 等第三方 tweak hook class_getSuperclass 导致重入） |
| 6 | 0.3.5 | 分类管理支持单独开关：开关移至行首左侧，可独立启用/禁用每个分类；关闭后仅影响「添加控制项」面板分组，已添加模块不受影响 |
| 7 | 0.3.6 | 分类管理行内布局修正为「名称靠左 … [开关] ☰ 均在右侧」；删除「关闭全部显示」行，表格由两段合为一段 |
| 8 | 0.3.7 | 「连接」分类新增 7 个独立模块（飞行模式／蜂窝数据／隔空投送／个人热点／蓝牙／无线局域网／VPN）：自研最小 bundle，默认 1×1，支持拖到 1×2 / 2×2 |
| 9 | 0.3.8 | 修复 0.3.7 连接的四个问题：长按无响应（判定改为「所有自研模块都响应」，无原生控制器的走自研面板）；1×2/2×2 中间多一个图标（隐藏原生内容改为两级遍历的兜底通道）；蜂窝数据不显示高亮（CoreTelephonyClient 无 +sharedInstance，改为 initWithQueue: 构造 + C API + 代理字典三级读取；乐观高亮无条件写入；新增 setSelected: 转发路径）；配色对齐 iOS 18/26（选中态改为品牌色实心圆角底 + 白色图标文字） |
| 10 | 0.3.9 | 「无线局域网」「蓝牙」改为 iOS 18 白色高亮交互样式（选中白底蓝图标/深色文字，未选中保留磁贴原生模糊底 + 白色图标；图标点位与文字布局按 1×1／2×1／2×2 三档分别排布；蓝牙下行显示已连接设备数，无连接显示「打开」；WiFi 未联网显示「打开」）；两者改为单实例，添加后不再出现在「添加控制项」列表 |
| 11 | 0.3.10 | 高亮底由白改灰（#C7C7CC）并覆盖关闭状态，修复「关闭状态下的背景色缺失」；蓝牙 CAPackage 图标改为权威重绘 + 多轮后置重绘，对齐无线局域网蓝；蓝牙下行文案改为「N个设备已连接」；「隔空投送」「VPN」接入同一风格（灰底／蓝图标／三档排布／单实例），隔空投送副标题中文化为「仅限联系人／所有人／关闭」，VPN 副标题为「已连接／未连接」 |
| 12 | 0.3.11 | 背景色回归全局约定：打开=纯白、关闭=计时器同款灰（colorWithWhite:1.0 alpha:0.12 + 原生模糊），撤销 0.3.10 双态 #C7C7CC 错误灰底；2×2 底部文字与图标统一 16pt 内边距对齐；蓝牙关闭态文案「打开」修正为「关闭」，无线局域网关闭态「Off」修正为「关闭」；「飞行模式」「蜂窝数据」「个人热点」接入同一白/灰风格与三档排布（飞行模式黄图标，蜂窝数据/个人热点绿图标，蜂窝数据副标题为卡名），均为单实例；「添加控制项」预览磁贴同步白/灰双态 |
| 13 | 0.3.12 | 关闭态底色的根因修复：关闭态不再绘制任何自绘叠色，直接沿用磁贴自身的系统材质（与页面上其他模块完全一致）。根因是原生内容隐藏通道会把模块的 MTMaterialView 一并隐藏（该类不是 UIVisualEffectView，通道不豁免），0.3.9 的「关闭态背景缺失」与 0.3.10/0.3.11 反复调灰都是它的衍生症状；现在显式恢复材质，并放宽材质查找（MTMaterialView / 子类 / UIVisualEffectView + 满磁贴尺寸判定）；若确实取不到材质则补一块 Echo Reborn 自身的 SystemUltraThinMaterialDark 毛玻璃底，任何情况下都不再出现纯色叠层 |
| 14 | 0.3.13 | 修复 0.3.12 关闭态「背景直接没了」：材质查找放宽后，命中了一个嵌在「已被隐藏的父容器」里的满尺寸材质，把它 un-hide 仍然不可见，于是兜底毛玻璃也没被创建。改为按「可用性」解析：仅当模块自带材质是磁贴**直接子视图**（父级可见、un-hide 必然生效）才沿用，否则一律使用 Echo Reborn 自有的 SystemUltraThinMaterialDark 毛玻璃底；并把毛玻璃置于自绘层之下，关闭态始终有一块真实材质、且不出现任何纯色叠层 |
| 15 | 0.3.14 | 关闭态材质加深：兜底毛玻璃由 `SystemUltraThinMaterialDark` 改为 `SystemThinMaterialDark`（原档在原生磁贴旁偏浅）；2×1 布局横向对齐重做：七个连通性模块统一采用「以蓝牙为基准」的共享左内边距——先由「蓝牙」二字在磁贴内水平居中反推该内边距，再让其余六个模块共用同一值（副标题与标题左对齐），仅改横向、垂直不变；其余非连通性展示层（快捷指令、Text Size、电源、缩小模块）保持原内边距不变 |
| 16 | 0.3.15 | 架构对齐上游拆分：七个连通性模块 bundle 从本包迁出至独立的 COSMIC Kit 包（`com.futur3sn0w.cosmickit`，仓库结构照搬上游 MoarTweaks/COSMICKit 的 `Modules/Connectivity/`「1 源文件 + 7 target」形态，类名改用上游名 ERAirplaneModule/ERWiFiModule/ERAirDropModule/ERCellularDataModule/ERBluetoothModule/ERHotspotModule/ERVPNModule）；本包 Makefile 移除 `SUBPROJECTS += modules`，删除 `modules/` 目录，`Depends` 增加 `com.futur3sn0w.cosmickit`；bundle identifier 命名空间 `com.strive.echoreborn.connectivity.*` 与安装路径 `/Library/ControlCenter/Bundles` 逐字不变，`Tweak.xm` 一行未改，界面零像素变化；论证与不变量见 `ARCHITECTURE-DIAGNOSIS.md` |
| 17 | 0.4.7 | 工作区基线版本：0.3.15 之后至 0.4.7 的迭代未在本表续记（详见 lvxl456/Echo Reborn 的 0.5.6.x Release 记录）|
| 18 | 0.5.6.21 | 修复「设置入口点不动」：独立模块配置页的按钮 action 选择器未在接口声明、失败静默返回；现已声明并补日志 |
| 19 | 0.5.6.22 | 单击震动反馈（观察型 tap 识别器 + 两处手势代理放行、三档强度、设置入口与自检）；关闭液态玻璃无背景（`ERSyncConnectivityTileChrome` 背景不变量）|
| 20 | 0.5.7 | 工作区落地 0.5.6.21/22 三项修复：独立模块背景不变量重挂（布局路径 + 切换液态玻璃后重同步 `ERSyncConnectivityTileChrome`，日志 `OFFBGFIX`）、模块按钮单击震动 + 三档强度（日志 `TAPHAPTIC`）、设置入口与自检；修正自检生成器释放过早（改用关联对象保留）；补 `ModuleEnabled_<id>` 过滤「添加控制项」。另含三处**编译修正**（首次真实出包）：① `%new` 方法的参数类型不得带 `__unused`——Logos 会把签名复制进生成代码，触发 `-Werror,-Wignored-attributes`；② 对仅 `@class` 前置声明的 `CCUIButtonModuleView` 不得直接发送消息：`[self x]` 报「receiver type is a forward declaration」，`[(id)self x]` 报「no known instance method」（`%new` 只在运行时注册、不向编译器暴露声明），故 tap 安装改为文件级静态函数 `ERInstallModuleTapFeedback(UIView *, id)`。版本号由 0.5.6.23 改为 **0.5.7**：0.5.6.23 的 tag/Release 已被占用且 asset 与本次产物不符（655 KB vs 594 KB）、tag 指向旧提交 `f05d526`，改号可避免覆盖历史发布。`control`/`Info.plist` 同步 0.5.7 |
| 21 | 0.5.8 | 关闭态背景改为「系统模块同款材质」（克隆系统 platter 替代自造合成模糊，`OFFBGSRC` / `OFFBGUPGRADE`）；修复「点模块无震动」（观察型 tap 识别器 delegate 未设 → 放行分支成死代码，动作目标改为 `EchoRebornCoordinator`、宿主改为 `CCUIContentModuleContainerView`，`TAPHAPTIC` / `TAPHAPTIC-OK`）；阻止 GlassKit 路由二次玻璃化 Echo Reborn 自有兜底材质 |
| 22 | 0.5.9 | **液态玻璃三处根因修复**：① 个人热点可用性误判（`SBTetheringController`/`TetheringManager` 在 iOS 16/17 不存在 → `avail=0` → 材质被压到 0.52 透明度，成为唯一偏暗磁贴；改为复用蜂窝可用性判定）；② 磁贴显式玻璃的 `groupName` 由固定串改为 `nil`（与路由一致，七块玻璃此前共用同一 backdrop 分组 → 渲染异常）；③ 抽出 `ERGlassRadiusForModuleMaterial()` 让路由与磁贴玻璃共用同一圆角算法，并补 `masksToBounds` / `cornerCurve`；④ 玻璃装上后隐藏兜底材质（单一背景面，消除双层不同圆角叠加产生的弧线）；⑤ 玻璃同步移出「材质首次创建」分支独立为 `ERSyncConnectivityTileGlass()`，实时开关液态玻璃即刻生效（新增 `OFFGLASS` 标记，`OFFBGFIX` 增加 `glass=` / `r=` 字段）|
| 23 | 0.5.10 | **① 开启液态玻璃后七个模块透明缺背景**：0.5.9 自造 `LGCreateRegisteredGlass` 并插在磁贴包装视图最底层，缺少 GlassKit 给系统模块的 1.5/3/5/8/12s `applyFilters` 重试（滤镜挂不上即全透明空层），层序也与系统模块不同；改为调用系统同款注入函数 `LGInstallRegisteredGlassInMaterial()`（同一分组名/滤镜/层序/重试），并给玻璃打 `kERConnectivityTileGlassTag` 以免被原生内容隐藏pass关掉；圆角改取材质短边一半（= 磁贴圆角 31.5/35.5，此前取模块容器 37.5）；`ERRestoreConvertedTileMaterial` 的隐藏判据由「玻璃存在」改为「`lgFilterAttached`」，保证磁贴永远有可见背景（`OFFBGFIX` 增加 `fa=` 字段）。**② 音乐模块 4x1 空白 / 退出编辑态异常**：通用分支判据 `size < natural` 把 4x1（自然 2x2）误判为「被缩小」，触发缩略接管（隐藏两级原生子视图 + 换纯图标磁贴）且状态残留到注销重登；新增 `nowplaying` 专属分支走原生 `ERConfigureNowPlayingLayout`（`MEDIAFIX` 标记）；`ERSetModuleNativeContentHidden(NO)` 改为遍历整棵子树还原（原来只还原一级）；`ERMediaModeForNowPlayingView` 优先采用已保存网格尺寸而非瞬时 bounds。**③ 七个模块图标统一**：新增共享图标外框 `kERConnectivityGlyphBox = 30pt`，`ERUniformConnectivityGlyphImage` 由「最长边=36」改为「等比居中放入固定方框」；兜底 SF Symbol 字号统一为 `kERConnectivityGlyphPointSize`（原为每符号 27~31）；蓝牙 CAPackage 由 67pt cell 方框改为同一 30pt 方框并对齐到 SF 墨迹位置 |

| 24 | 0.5.11 | **修复 0.5.10 的 watchdog 超时（反复注销、进不去桌面）**。0.5.10 在布局热路径上引入了五处新开销/回环：① `ERUniformConnectivityGlyphImage` 去掉了「尺寸已符合即原样返回」的短路，且调用点无条件把新建的 UIImage 赋给 UIImageView → 内容变更使视图再次请求布局 → 布局又进来再造一个新对象，形成**布局反馈回环**，主线程被占满 120 秒即被 watchdog 判死（主因）；② 音乐模块分支在布局回调里调 `layoutIfNeeded`，与 `MRUNowPlayingView` 的 `layoutSubviews` hook 形成回环；③ 磁贴玻璃每次刷新补一次 `applyFilters`（渲染服务往返）；④ 旧玻璃清理沿整条祖先链做 `viewWithTag:` 全子树深搜（O(子树 × 祖先)）；⑤ `ERSetModuleNativeContentHidden(NO)` 由两级改为遍历整棵子树。修复：恢复尺寸短路并在调用点用尺寸做闸门（只在结果尺寸变化时才赋值）；`layoutIfNeeded` 改回 `setNeedsLayout`；移除每次 `applyFilters`（保留 GlassKit 自带的 1.5/3/5/8/12s 重试）；清理限为两层的直接子视图；恢复严格限制两级。另新增 `LAYOUTGUARD` 重入闸门（深度 ≥ 2 直接返回），保证最坏情况下插件让路而非拖死 SpringBoard |

| 25 | 0.5.12 | **已废弃 / 被回退**。尝试把玻璃圆角改为取磁贴自身圆角、并用绕过 `-[MTMaterialView setHidden:]` 子类 hook 的方式保持材质可见作为 backdrop 取样源。实机效果与预期不符，用户要求完整回退到 0.5.11；0.5.13 的源码树中不含本版任何改动。 |
| 26 | 0.5.13 | 基线**完整回退到 0.5.11**，在其之上做两处修复。① **音乐模块 4×2 的液态玻璃圆角**：4×2 容器 304×146 被 `ERGlassIsModuleCandidate()` 判为非近正方形，圆角掉到 pill 分支（短边一半 73pt），而 4×4（304×304）走 `ERGlassModuleCornerRadius()`，两者相差一倍。新增 `ERGlassModuleIsNowPlaying()` / `ERGlassNowPlayingCornerRadius()`，仅当模块为 `com.apple.mediaremote.controlcenter.nowplaying` **且**形状为横向拉宽的大模块（宽>100 且 高>100 且 宽>高）时复用 4×4 的取值；其余模块（含七个连通性磁贴、2×2 / 2×4 音乐模块）判定路径一行未动。② **翻页多余位移**：`-applyRestingModuleOffsetToOverlay:` 写 `sublayerTransform` 时漏了 `gERPagerInteractiveTranslation`，而它被 `%hook UIViewController -viewDidLayoutSubviews` 在 `ERInteractivePagingGeometryActive()` 为真时反复调用（整段翻页恒为真），于是翻页中一次布局回调就把页面写回「未跟手」的位置——向下滑时先向上蹿一下再被拉回，向上滑同理。修复：算式补上跟手位移项，**且**翻页期间跳过静止态几何补写；新增 `[PAGER] flip start=…` 诊断日志。 |
| 27 | 0.5.14 | **0.5.13 实机无效果后的重新定位 + 探针版**（基线仍为 0.5.11）。用户反馈两问题「依旧没有任何变化」，据 0.5.13 日志复盘：`span=968.0`、`[PAGER] skip resting reassert during flip translation=0.0` **仅出现 1 次且位移已归零** → 0.5.13 对问题②的「静止态几何被重复补写」推测并非成因；`gERUseNativeOpeningCompensation` 经查为编译期常量 `NO`，开场补偿路径是死代码。日志另显示每次翻页后都跟一轮完整模块重排（`tileR` 在 31.5/35.5 间跳、`sync: 0 owned tile(s)` 重打印）。① **音乐 4×2 圆角重做判定**：0.5.13 的 `ERGlassModuleIsNowPlaying()` 在 responder 链上遇首个控制器即 `break`（容器控制器自带 identifier，非 nowplaying），且子树搜索仅 3 层（`MRUNowPlayingView` 更深）→ 恒返回 NO。改为**遍历整条 responder 链不提前 break** + 命中 `MRUNowPlaying*` 类名前缀 + 子树搜索加深到 6 层；并新增 `NPGLASS` 探针日志（值变化时才打印，记录模块网格尺寸与最终圆角）。② **翻页几何探针**：新增 `ERLogPageGeometry()`（`PAGEGEO` 标记，在 `-applyPageTransformToOverlay:` 进/出各打印一次 `sub/presSub/ty/by/pbY` 的模型值 vs 呈现值）、`-animatePageSettleForOverlay:` 内 `PAGESETTLE` 日志（记录 settle 起止的 `sublayerTransform.m42` 与时长）；并把 `-applyPageTransformToOverlay:` 里逐帧的 `ERRestoreNativeScrollBaseline(overlay)` 改为**仅在非翻页态调用 `if (!gERPagerTransitionActive)`**（翻页中不再每帧钳制原生滚动基线）。版本号 `ver=0.5.13` → `ver=0.5.14`（8 处）。 |
| 28 | 0.5.15 | **横屏控制中心折行**。现象：横屏下内容居中、只有顶部约 4 行可见，其余被裁在屏外。根因（源码定位）：① `ERGridVisibleWidth()` 把网格宽度硬编码为 `kERGridCellSize*4 + kERGridGap*3`（304pt / 4 列），而 `-frameForLayoutRect:` 用它做水平居中补偿 → 横屏（窗口约 812pt）里一块 304pt 网格被居中，两侧各留约 254pt 空白；② `ERVisualPageSpan()` / `ERVisualPageSpacerRows()` 的高度取自 `UIScreen.mainScreen.bounds`，而 **iOS 8 起该值不随转屏变化**、恒为竖屏尺寸 → 横屏时页面仍按竖屏算成 632pt+（8 行）高，而横屏可视高仅约 320pt，于是只有顶部约 4 行在屏内、行 5–8 被裁；③ 全文件 grep `willTransitionToTraitCollection` / `traitCollectionDidChange` / `viewWillTransitionToSize` 零命中 —— Echo Reborn 从来没有横屏分支。修法（配置空间不变，仍是一页 4 列 × 8 行，只在渲染层折行）：① 新增 `kERLandscapeBlockRows/Columns = 4/4` 与 `ERLandscapePresentationActive()`（从 overlay 所在**窗口** bounds 判方向）；② `ERGridVisibleWidth()` 横屏返回 8 列宽；③ `ERGridHorizontalCenteringCompensationForFrame()` 改为「目标左边 − 原生左边」，横屏允许负位移（竖屏代数等价、保留 `MAX(0,·)`）；④ `-layoutView:layoutRectForSubview:` 折行 —— 行 4–7 搬列 +4、行 −4，页面跨距改 4 行；⑤ 新增 `ERLayoutPageStrideRows()`，`ERVisualPageSpan()` 横屏取 4 行步长（翻页阈值/落位/指示器自动跟随）；⑥ `-gridBaseOriginForOverlay:` 与编辑网格的反推处按同样换算（否则编辑/拖拽落点整体左偏 4 个步长）；⑦ `-frameForLayoutRect:` 新增 `LANDSCAPE` 诊断日志（关键量 `nativeBaseX = 原生x − 列×步长`，各列恒等即证明原生对列 4–7 为线性外推）。预期：横屏排成 8 列 × 4 行（左 4 行 + 右 4 行），一页仍是 8 个 2×2 位、全部可见；竖屏分支一字未动。 |
| 29 | 0.5.16 | **横屏纵向定位（居中）**。0.5.15 折行生效后用户反馈「显示已没有问题，但整体位置过于靠下」。实测（竖屏原生 1290×2796 截图 → 旋转 90° 还原横屏，逐像素量边界）：横屏 932×430pt，内容块 694×340pt，**水平左 119 / 右 119（正好居中）**，但**垂直顶部空 101pt、底部只剩 22pt**（恰为 Home 指示条安全区）→ 内容整体贴底、顶部一大块空白。根因：Echo Reborn 只有 `ERGridHorizontalCenteringCompensationForFrame()`，**纵向一个补偿都没有**，纵向基准完全由原生给出，而原生仍按竖屏那套算。修复：新增 `ERGridVerticalCenteringCompensationForFrame()`，在 `-frameForLayoutRect:` 里仅横屏时把「4 行块」改为在容器内居中 —— 反推 `nativeBaseY = 原生 y − 行 × 步长` 得到原始基准，目标位 `centered = (containerHeight − extent)/2`（`extent = 4 行 − 1 个间距`，行间不留尾隙），并对顶部安全区（`window.safeAreaInsets.top`）与底部安全区（`bottom`）双向夹取，保证既不压状态栏带也不越过 Home 指示条、不出屏被裁；放不下时优先保住顶部安全区。竖屏分支一字未动。新增 `LANDSCAPEV` 诊断日志（容器高 / 上下安全区 / extent / 实际位移），`LANDSCAPE rect` 行补 `nativeBaseY` / `shiftX` / `shiftY` / `finalX` / `finalY`。注：实测该横屏截图顶部 101pt 为纯背景渐变，原始竖屏顶条也只有灵动岛——**横屏（iPhone）iOS 本身不显示状态栏**，故「顶部状态栏信息」在横屏并不存在，如需强制显示属独立改动。 |

| 30 | 0.5.17 | **横屏顶部 chrome 归位**。用户三点需求：① 顶部按钮横屏要显示在两边、位于状态栏下面；② 返回 App 胶囊与状态栏保持一致；③ 状态栏整体上移、控制中心完整显示。逐像素实测（横屏 2774×1279px ≙ 932×430pt，竖屏 1279×2774px ≙ 430×932pt，同一台设备）：横屏状态栏本体 y 76.9–119.3pt 偏下；「+」字形 y 0–10.1pt（中心 4.6pt，被顶部裁掉约 15pt）；**电源键字形 x 380.7–394.8pt —— 与竖屏完全相同**，而竖屏 x = 430−22−20 = 388 正是该值，即 host 宽度仍按竖屏 430pt 计算；竖屏对照「+」中心 45.7pt、状态栏 104–147pt。两个根因：① `-installQuickAccessHostOnOverlay:`（7791）用 `viewWithTag:181000` 做 early-return，**host 只创建一次**，而宽度约束 `constraintEqualToConstant:CGRectGetWidth(controller.view.bounds)` 在安装那一刻定死 —— 实测横屏下 `controller.view.bounds` 仍是竖屏 430pt（与 `UIScreen.mainScreen.bounds` 同一类陷阱），只有窗口 bounds 会转屏，于是**先在竖屏装好再转横屏时宽度永久停在 430pt**；② `ERQuickAccessCenterYOffset()`（4577）取 `safeAreaInsets.top`，横屏该值为 0 → 落到硬编码 56 兜底 → 偏移 −28，把 40pt 按钮顶出屏外。修法：① 新增 `ERQuickAccessHostWidth()`（优先窗口宽度），宽度约束留句柄，在 early-return 分支调用新增的 `-updateQuickAccessGeometryForHost:overlay:` 原地刷新宽度与两枚按钮的纵向基准（借助 16314 已有的「每次布局都重装 host」路径，无需新增转屏 hook）；② 横屏给独立基线 `kERLandscapeCornerButtonDrop = 76.0`（叠加在最后，保留用户已有的 `QuickAccessVerticalOffset` 设置）；③ 新增 `-applyLandscapeChromeRiseForOverlay:` + `-applyLandscapeRise:toView:`，把 `CCUIHeaderPocketView` / `CCUISensorAttributionPrivacyHeaderView` 上移 `kERLandscapeStatusBarRise = 74.0`，胶囊与状态栏同容器故自动保持一致；基准 transform 单独存 key 保证幂等，竖屏 rise=0 自动还原。**关键约束**：host 是 header pocket 的子视图，整体平移 pocket 会把按钮一起带走并抵消下移量 → 装着 host 的容器改为只平移其余子视图。几何账（430pt 高 − 底部安全区 21 = 409）：状态栏 42 + 模块块 340 = 382，容不下三层顺排，故按钮走左右侧边沟（模块块宽 692、屏宽 932 → 左右各余 120pt），不与模块重叠。目标排布：状态栏 y 2.9–45.3、按钮 y 60.6–100.6、模块块 y 45–385。新增 `LANDSCAPECHROME` 诊断（记录实际位移的类名、rise、frameY、高度与 host 宽度）。注：`chromeViewsInOverlay:` 等既有 capture/restore 路径可能还原 transform，故位移在每次布局回调与 0.18s 延迟回调里重复重写（与材质抑制同一套路），翻页/动画期间不改写，避免与 CC 自身开合动画抢属性。 |

| 31 | 0.5.18 | **横屏状态栏改成「绝对目标位」+ 一次性顶部 chrome 视图树诊断**。用户横屏截图（1133×522px ≙ 932×430pt，x/y 比例 1.2157/1.2140）上用红框标注目标位，框内手写红字逐像素识别为「状态栏…在这个位置」（10 个 13×13px 汉字，逐字模板匹配：状 0.835 / 态 0.845 / 栏 0.871 / 在 0.776 / 这 0.704 / 个 0.835 / 位 0.825 / 置 0.451）。实测红框 = **x 66.6–863.7pt、y 16.5–46.1pt**（中心 y 31.3、高 29.6，水平两侧留白 66.6 / 68.3 基本对称）；同一张截图里状态栏图标带（左时间簇 x 73–111、右信号/电量簇 x 796–856，电量圆角框 x 827–855）落在 **y 50.0–78.0pt、高 28pt** → 与目标正好差 **33.5pt**。根因：0.5.17 用的是固定量 `kERLandscapeStatusBarRise = 74.0`，它是按「自然 top≈76.9pt」估出来的；而实测「抬完 74 之后仍在 y 50」→ 真实自然基准 top ≈124pt，估值偏小约 47pt。固定量与真实基准无关，只要机型/系统/首次呈现让它漂一点，落位就会跟着漂。修法：删掉固定量，新增 `kERLandscapeStatusBarTargetTop = 16.5` 与 `kERLandscapeStatusBarMaxRise = 200.0`，新增 `-landscapeChromeRiseForContainer:landscape:` —— 先把 transform 还原成基准、用 `convertRect:toView:window` 量出**窗口坐标系下的基准顶边**，再反算 `rise = baseTop − 16.5`（夹取 0…200），于是基准 top 是 124 也好、50 也好，都落到同一绝对位置，且重复调用幂等；同一容器的所有子视图共享同一个 rise，相对关系不会被拉开。同时把命中集从 `HeaderPocket` / `SensorAttribution` 扩到**类名含 `StatusBar` 的容器** —— 0.5.17 只认前两类，若真正的状态栏挂在别的容器里，被平移的就是已被剥掉背景材质的空壳，现象正是「改了但没动」。「+」/电源的侧沟排布与模块块居中本版不动（抬完状态栏后按钮顶边 52pt 仍在状态栏底边 46pt 之下，无需联动）。新增 `CHROMADUMP` 一次性诊断（每个横屏会话只打一次，回竖屏复位）：先打方向判定输入（`gate` / 窗口 bounds / overlay view bounds / UIScreen bounds / 上下安全区 / overlay 数 / 命中数），再把**顶部 140pt 内、宽>4 高>1 的视图**按窗口坐标 y 排序打最多 48 条（`y h x w a hid matched cls`），诊断先于改写执行，读到的是原生/基准位置 —— 下次日志可直接读出状态栏挂在哪个类上。版本号 `ver=0.5.17` → `ver=0.5.18`（18 处），`control` 0.5.18，`Info.plist` 0.5.18 / build 32，校验脚本 `_verify_deb.py` 同步（新增 `CHROMADUMP` 必检项）。 |


| 32 | 0.5.19 | **修掉 0.5.18 横屏状态栏「改了但完全没动」**。用户 `Echo Reborn-log-20260915-133934.txt`（ver=0.5.18）+ 12:30 横屏截图实证：`CHROMADUMP ... gate=1 win=932.0x430.0 ... matched=2` 说明代码跑了、方向也认对了，但 `LANDSCAPECHROME` 只有隐私指示条那条 `rise-plan cls=CCUISensorAttributionPrivacyHeaderView ... rise=41.5`，状态栏本体从头到尾没有过任何位移。**两个根因叠加**：① 找状态栏用的命中串是 `containsString:@"StatusBar"`，而 CC 里这条通栏状态栏（双卡 主号/副号 + WiFi + 蓝牙 + 电量）的真实类名是 **`CCUIStatusLabel`** —— 是 StatusLabel 不是 StatusBar，**从来没被匹配到**；② 唯一匹配上的 `CCUIHeaderPocketView` 顶边本来就是 0，反算 `rise = 0 − 16.5 < 0` 被夹到 0，**即便匹配上也不会动**。修法：命中集补 `StatusLabel`；`HeaderPocket` 不再整体平移（顶边=0 平移无意义），改成「容器」处理 —— 用新增的 `-isLandscapeStatusBarLikeView:inWindow:` 挑它里面**宽 ≥ 窗口一半、高 ≤ 60pt**的直接子视图平移（实测 pocket 子视图：`MTMaterialView` 高≈90pt 被高门槛排除、「+」/电源按钮宽 40pt 被宽门槛排除、`CCUIStatusLabel` 宽 932 高 44 命中），并置 `clipsToBounds = NO` 避免移出 pocket 顶边后被切半截。**rise 改用固定量 62pt**：0.5.18 未生效态等同原生位置，横屏截图逐像素实测状态栏内容带落在 **y 86.0–100.8pt（中心 93.4）**，用户标注目标框 **y 16.5–46.1pt（中心 31.3）** → 差值 62.1pt；平移量与坐标系原点无关（窗口偏移被约掉），而 `convertRect:toView:window` 在 CC 呈现动画期间会漂（同一容器量到过 **-12 / 58 / 12** 三个 baseTop，0.5.18 的反算 rise 因此是废的），故不再反算。**新增系统状态栏窗口兜底**：iOS 在 CC 里显示的这条状态栏未必挂在 overlay 子树里，也可能在独立的 `UIStatusBarWindow`（0.5.18 只扫 overlay，这也是够不到它的原因之一）→ 新增 `-applyLandscapeRiseToSystemStatusBarExcludingWindow:`（按窗口类名含 `StatusBar` 精确限定、只在「CC 呈现 + 横屏」时平移）与 `-restoreLandscapeSystemStatusBarIfNeeded`（在 `SBControlCenterController` 状态 0 的收起清理点调用），被移过的视图登记在 `gERLandscapeSystemStatusBarMoved`，避免把 App 自己的横屏状态栏一起带偏。诊断升级：`CHROMADUMP ver=0.5.19` 改为**先打全部窗口清单**（`cls y h w lvl hid key`）、再打所有命中视图（`cls y h x w a hid win`），修掉 0.5.18 那份「只扫 overlay 顶部 140pt 且截断在 48 行」导致看不见真凶的问题。版本号 `ver=0.5.18` → `ver=0.5.19`（18 处），`control` 0.5.19，`Info.plist` 0.5.19 / build 33，校验脚本 `_verify_deb.py` 同步（新增 `ver=0.5.19`、`statusbar-rise`、`system-statusbar` 必检项）。 |
| 33 | 0.5.20 | **设置页重构（卡片式 + 彩色图标 + 增强设置内联）**。根页从 28 项收敛为 7 入口（启动插件 / 增强设置 / 液态玻璃 / 分类管理 / 快捷指令管理 / 模块管理 / 诊断日志），全部带左侧彩色圆角图标（ERUIHelpers 运行时生成 SF Symbol 圆角芯片，9 色映射）；删除全部分组 footer。原根页散落的「调节控件 / 震动反馈 / 快捷按钮位置」三个分组并入新增的「增强设置」子页（EREnhancedSettingsController + Enhanced.plist），子页内联四分区：控制中心分页（分页开关）、操作按钮（快捷操作按钮开关 + 垂直偏移滑块）、调节控件（音量/亮度开关）、震动反馈（单击震动反馈开关在上方、震动强度三档在下方）。震动强度改为本页内联的三段控件（ERSegmentedCell，轻/中/重 绑定 TapHapticStrength，0/1/2），删除原独立「震动强度」子页与「震动自检」入口。版本号 0.5.19 → 0.5.20，`control` 0.5.20，`Info.plist` 0.5.20 / build 34，`Tweak.xm` 日志 `ver=` 同步 0.5.20。 |
| 34 | 0.5.21 | **设置页三处实机问题修复**（依据用户崩溃日志 `Preferences-2026-09-15-173741.txt`）。① **点「增强设置」闪退**：`ERSegmentedCell` 用 `self.controller` 取所属列表控制器，而 **PSTableCell 在 iOS 16/17 上没有这个访问器**（某些头文件里那条声明与运行时不符）→ `-[ERSegmentedCell controller]: unrecognized selector sent to instance`；崩溃点逐帧吻合：`PSListController -tableView:cellForRowAtIndexPath:` → `-refreshCellContentsWithSpecifier:` → 未识别选择器，`termination Abort trap: 6`。改为从 specifier 的 **`target`** 取（`loadSpecifiersFromPlistName:target:` 传入的就是该控制器）：访问器存在就直接取，否则退回 KVC 读 `_target`；若仍取不到，则直接写偏好域并补发 `PostNotification` 声明的 Darwin 通知，不做静默失败。② **左侧图标不显示**：运行时给 specifier 挂图片的键是 **`iconImage`（UIImage）而不是 `icon`** —— plist 里的 `icon` 是「文件名」字符串，由 `SpecifiersForPlist` 载入时翻译成 `iconImage`，运行时 `setProperty:forKey:@"icon"` 没有任何代码会读它。`erApplyIcons` 已改键；根页 5 个入口补 `alignment=0`（左侧对齐）。**中途踩坑记录（勿重犯）**：曾试写 `ERIconButtonCell : PSButtonCell` 自定义入口行（想把标题改成深色、图标贴左 16pt），Theos vendor 头文件下**编译不过**——`Preferences/PSTableCell.h:19` 把 `PSButtonCell` 声明为 `PSCellType` 枚举成员，同名标识符已是枚举常量，于是 `@interface PSButtonCell : PSTableCell` 报 `redefinition of 'PSButtonCell' as different kind of symbol` + `reference to 'PSButtonCell' is ambiguous`；同时 `__has_include(<Preferences/PSButtonCell.h>)` 为假（vendor 头文件集不含该类接口）。结论：**这个 SDK 头文件集无法用编译期继承 PSButtonCell**；确实需要自定义入口行时，只能在运行期用 `objc_allocateClassPair(NSClassFromString(@"PSButtonCell"), "…", 0)` 建子类（plist 的 `cellClass` 本就按名字解析，可行），或从控制器侧 `cellForRow…` 调外观。③ **文案去重**：删掉「增强设置」分组标题（与入口同名）、`快捷指令管理` 改名为 `快捷指令` 并删同名的「快捷指令」分组标题；两处 PSGroupCell 仍保留以维持卡片分隔。版本号 0.5.20 → 0.5.21，`control` 0.5.21，`Info.plist` 0.5.21 / build 35，`Tweak.xm` 日志 `ver=` 同步（22 处），校验脚本 `_verify_deb.py` 同步（`ver=0.5.21` + 新增 `iconImage` / `ERSegmentedCell` 必检项）。**另修一个本轮引入的编译错误**：`ERSegmentedCell` 里取控制器必须先声明类型，`id controller = …` 会因 ARC 下找不到方法签名而报 `-Werror: no known instance method for selector 'readPreferenceValue:'` / `'setPreferenceValue:specifier:'`，必须写成 `PSListController *controller = …`（并 `#import <Preferences/PSListController.h>`）。 |
| 35 | 0.5.22 | **① 震动强度改「直读偏好域」（消除 0.5.21 的显示值不确定）**：0.5.21 用 specifier 的 `target` 取控制器，若 `PSSpecifier` 既无 `-target` 访问器也无 `_target` ivar 就会退到 plist 的 `default`，表现为「显示值不准但不崩」。本版读、写全部改为 `CFPreferences` + specifier 自带的 `defaults` / `key` / `PostNotification`，与 `Tweak.xm` 的 `CFPreferencesCopyAppValue(CFSTR("TapHapticStrength"), CFSTR("com.strive.echoreborn.preferences"))` 是**同一个存储**；`ERSegmentedCell.m` 现在不触碰任何私有 API，也不再需要 `PSListController` 头文件。**② 入口行改系统设置外观**：新增 `prefs/EREntryCell.m/.h`，在 `+load` 里用 `objc_allocateClassPair(NSClassFromString(@"PSButtonCell"), "EREntryButtonCell", 0)` 建运行期子类，覆写 `refreshCellContentsWithSpecifier:` 与 `layoutSubviews`（先调 super，再覆盖 `textLabel.textColor = labelColor` 与 `accessoryType = DisclosureIndicator`）；`Root.plist` 5 个入口行加 `cellClass=EREntryButtonCell` 并**保留 `cell=PSButtonCell`** —— 万一运行期注册失败即回落到 PSButtonCell，入口照样能点开，不会退化成「点了没反应」。这正是迭代 34 里记录的「vendor 头文件无法编译期继承 PSButtonCell」的正解（该记录已预判此路径）。**③ 0.5.21 崩溃日志复核结论**：用户 18:19:58 的日志与 17:37:41 的 `_crash_0.5.20.txt` 是**同一个二进制**（`EchoRebornPrefs` 崩溃帧同为 `+70320`、缺失选择器同为 `controller`），而该崩溃进程 `procLaunch=17:38:52` 早于 0.5.21 出包（18:09）与发版（18:10）—— 即 **0.5.21 从未在设备上被执行过**，并非「修了还崩」。静态佐证：0.5.21 二进制里 `controller` 字符串出现 0 次（0.5.20 为 2 次），并新增 `iconImage`（2 次）。版本号 0.5.21 → 0.5.22，`control` 0.5.22，`Info.plist` 0.5.22 / build 36，`Tweak.xm` 日志 `ver=` 同步（22 处），`_verify_deb.py` 同步（版本闸门 + 新增 `EREntryButtonCell` / `ER-seg-0522-directprefs` 必检项）。 |
| 36 | 0.5.23 | **① 增强设置「操作按钮 → 垂直偏移」滑轨对齐（用户三项需求之一）**：新增 `prefs/ERSliderCell.m/.h`，在 `+load` 里用 `objc_allocateClassPair(NSClassFromString(@"PSSliderCell"), "ERSliderTrackCell", 0)` 建运行期子类，覆写 `layoutSubviews` 与 `refreshCellContentsWithSpecifier:`（先调 super 再改几何，基类 IMP 在注册期用 `method_getImplementation` 缓存后直接调用，不用 `struct objc_super`）。**为什么必须走运行期建类**：与迭代 34/35 同因 —— `Preferences/PSTableCell.h:19` 把 `PSSliderCell` 也声明为 `PSCellType` 枚举成员，编译期 `@interface … : PSSliderCell` 会撞同名符号。**几何规则（全部相对实测，不写死绝对值）**：轨道左端拉到标题 `UILabel` 左边缘（`titleAbove` 判定：仅当 `CGRectGetMinY(track) >= CGRectGetMaxY(title) - 1` 即标题确实位于轨道上方时才生效 —— 若某 iOS 版本把标题与轨道排成同一行 inline，该规则自动失效，不会出现轨道横穿标题的重叠）；右端一律收进 `contentView` 内容区 `kERSliderTrailingInset`(16pt)，即「不超过按钮范围」；读数标签（按「文本全为数字/负号/小数点」原地定位，不依赖私有 ivar）改 `NSTextAlignmentCenter` 并水平居中于内容区，纵向位置保持原生不动。**两个可调常量**：`kERSliderLeadingInset`(16，找不到标题时的左内边距兜底)、`kERSliderTrailingInset`(16，右端留白) —— 真机看完不满意只改这两个数。`Enhanced.plist` 该行加 `cellClass=ERSliderTrackCell` 并**保留 `cell=PSSliderCell`**（注册失败即回落原生实现，滑块照样能拖、值照样能存，只是没有对齐/居中）。**② 根页三项入口合并为同一卡片（用户三项需求之三）**：`Root.plist` 里「分类管理 / 快捷指令 / 模块管理」三行从原先的三个 PSGroupCell（`控制项分类` / 无标题 / `独立模块`）并入**同一个** `控制项分类` 卡片；删除 `独立模块` 分组标题与其空卡片（三行现在共用一个 PSGroupCell，卡片数 3 → 1）。**③ 横屏布局思路图（用户三项需求之二）**：另出独立文档 `Echo Reborn-横屏布局-思路图.html`，覆盖 32 个圆形元素（8 列 × 4 行折行）、状态栏（固定上移 62pt）、操作按钮（角标 drop 76pt）、定位/相机权限弹窗四部分，纯文档不改代码。版本号 0.5.22 → 0.5.23，`control` 0.5.23，`Info.plist` 0.5.23 / build 37，`Tweak.xm` 日志 `ver=` 同步（22 处），`_verify_deb.py` 同步（版本闸门 + 新增 `ERSliderTrackCell` / `ER-slider-0523-aligned` 必检项）。 |
| 37 | 1.0.0 | **项目更名 Echo Reborn + 设置页界面修复 + 新图标**（迭代 37 起版本线由 `0.5.x` 改为 `1.0.0`；0.5.24~0.5.27 未在本表续记）。**① 更名**：`package`/偏好域/通知名 `com.futur3sn0w.ccaster*` → `com.strive.echoreborn*`，dylib `CCAster.dylib`/`CCABackboardd.dylib` → `EchoReborn.dylib`/`EchoRebornBackboardd.dylib`，过滤器 plist `EchoReborn.plist`/`EchoRebornBackboardd.plist`，设置 bundle `CCAsterPrefs` → `EchoRebornPrefs`（含 PreferenceLoader plist 与图标文件名），类前缀 `CCA` → `ER`、方法/属性/plist 键前缀 `cca` → `er`（`erIcon`/`erIconColor`/`erValueSuffix`），日志目录 `/var/mobile/Library/Logs/EchoReborn`、`EchoReborn-log-`、`echoreborn.log`，Swift 头 `EchoReborn-Swift.h`；作者统一 `Strive`；显示名「Echo Reborn」。**刻意不动**：上游仓库路径 `MoarTweaks/CCAster`、上游第三方包 `com.futur3sn0w.cosmickit`（`control` 的 `Replaces` 仍在引用）、`futur3sn0w.github.io` 图标源、LiquidSiri 原版偏好域 `com.yourcompany.liquidsiri.prefs`（上游原版键名不加前缀，Tweak 侧一字节未改）。`control` 另加 `Conflicts: com.futur3sn0w.ccaster` + `Replaces: com.futur3sn0w.ccaster, com.futur3sn0w.cosmickit` —— 新旧两个 dylib 同时注入 SpringBoard 会重演 0.5.25 的重复类崩溃，必须声明冲突。**② 滑杆行重写（本次界面问题的根因）**：0.5.23~0.5.27 用 `objc_allocateClassPair(NSClassFromString(@"PSSliderCell"), "ERSliderTrackCell", 0)` 造运行期子类，但 **`PSSliderCell` 在运行期并不存在**（Theos 的 `Preferences/PSTableCell.h` 把它声明成 `PSCellType` 枚举成员，真正画滑杆的类由框架按 `cell` 名映射），于是实际退化成 `PSTableCell` 子类：`refreshCellContentsWithSpecifier:` 走普通 cell 分支 → **只有标题、没有滑杆也没有读数**，行高回落 44pt。真机截图（Soko 页 04:08、Siri 页 04:17）两页六个滑杆行全部只剩标题，即为该退化态。新版改为**编译期 `@interface ERSliderTrackCell : PSTableCell`** 自绘「标题（108pt 定宽列）｜滑杆｜读数（56pt 右对齐）」单行版式，全部手动布局、只用 `contentView.bounds` 一个坐标系（不再有 `convertRect` 换算），读/写走 `CFPreferences` + specifier 的 `defaults`/`key`/`PostNotification`（与迭代 35 的 `ERSegmentedCell` 同一套，不碰私有 API）；`layoutSubviews` 里若发现 `iconImage` 的 `UIImageView` 则整体右移一个图标宽度（增强设置那行带图标的滑杆不受影响）；读数格式为「整数且跨度 ≥ 10 用整数，其余一位小数」，缩放类三行由 plist 的 `erValueSuffix` = `×` 显示成「1.0×」。标题列定宽 → 每行滑杆左右边界完全相同，**宽度逐行一致**。**③ 标题去括号**：Siri 外观六个标题去掉全部括号及括号内文字（`垂直位置（上下偏移）` → `垂直位置`，`光球尺寸（缩放）` → `光球尺寸`，`宽度（缩放）` → `宽度`，`高度（缩放）` → `高度`）。**④ 行高**：三个 plist 的 9 个滑杆 specifier 全部加 `height` = 54（即使框架忽略该键回落 44pt，单行版式所有元素纵向居中，不会像双行版式那样被裁掉）。**⑤ 修复「从桌面回到设置后左侧图标消失」**：根因是 Preferences 回到前台会重新解析 plist 得到一批新的 `PSSpecifier`，运行时挂上去的 `iconImage` 随之丢失，而 `viewDidLoad` 一个生命周期只跑一次；新增 `-erActivateIconRefresh`（幂等注册 `UIApplicationDidBecomeActiveNotification`）+ 四个带图标控制器（根页/增强设置/锁屏控制项/iOS27 Siri）的 `viewWillAppear:` 重挂。**⑥ 新图标**：按用户设计稿（1920×1920，白底圆角方块 + 右下角水印）裁切圆角方块外接框、用「自边界洪水填充近白像素」抠掉底色与水印（内部白色高光不受影响），产出 `assets/icon.png`(1024)、`EchoRebornPrefs.png`/@2x/@3x(29/58/87，设置入口图标) 与新 `assets/banner.png`(2400×800)。版本号 `control` / `Info.plist` / `Tweak.xm` 日志 `ver=` 全部同步 1.0.0，`Info.plist` build 39 → 40；`_tools` 的推送/发布/校验脚本仓库常量改为 `lvxl456/EchoReborn`，产物校验改检 `ER-slider-1000-selfdrawn`。 |
| 38 | 1.0.1 | **设置闪退修复 + Sileo 图标修复 + 操作按钮两个新控件 + 关机键放射菜单 + 快捷指令磁贴定位**（依据用户 `Preferences-2026-09-16-045830.txt` 崩溃报告与 `EchoReborn-log-20260916-051128.txt`）。**① 反复进出设置闪退（SIGABRT）**：崩溃异常为 `-[? tableView]: unrecognized selector sent to instance`，调用链 `__CFNOTIFICATIONCENTER_IS_CALLING_OUT_TO_AN_OBSERVER__ → -[UIApplication _stopDeactivatingForReason:]`，即**回到前台**时通知投递到了已释放对象。根因：1.0.0 为修「图标回桌面后消失」给每个 `PSListController` 各 `addObserver:self`，却从不 `removeObserver` —— NSNotificationCenter 不持有观察者（unsafe unretained），控制器释放后观察者槽成悬垂指针，内存被别的对象复用后 `-tableView` 就打在它身上。修法：`prefs/ERUIHelpers.m` 新增 `ERIconRefreshRegistry` 单例（随进程存活）做**唯一**观察者，控制器只登记进 `NSHashTable` 弱引用表（释放即自动失效），并把刷新表格改为 KVC 取值 + `isKindOfClass:[UITableView class]` 校验，不再直接向对象发 `tableView`；`erApplyIcons` 也补了 `respondsToSelector:` 防御。**② Sileo 图标不显示**：`control` 原 `Icon:` 指向 `raw.githubusercontent.com/lvxl456/EchoReborn/...`，而仓库是私有的（实测该域名与 `github.com/.../raw/` 两个地址匿名请求均返回 404），Sileo 拉不到就退化成占位图。改为随包内置：新增 `prefs/Resources/icon.png`（512×512，取自 `assets/icon.png`），`Icon:` 改指 `file:///Library/PreferenceBundles/EchoRebornPrefs.bundle/icon.png`，装完不联网即可显示；设置列表入口图标 `EchoRebornPrefs.png/@2x/@3x` 不变。**③ 操作按钮新增两个控件**：`prefs/Resources/Enhanced.plist` 的「操作按钮」卡片下新增「横向缩进」(`QuickAccessHorizontalInset`，0–64，默认 22) 与「按钮大小」(`QuickAccessButtonSize`，24–64，默认 40)，均为 `ERSliderTrackCell` 自绘行、行高 54、带图标（`arrow.left.and.right` / `arrow.up.left.and.arrow.down.right`、purple）。Tweak 侧新增全局 `gQuickAccessHorizontalInset`/`gQuickAccessButtonSize` 与 `ERQuickAccessButtonInset()/ERQuickAccessButtonSide()`（区间夹取），按钮的 leading/trailing/width/height 四条约束改为读这两个值并存到按钮上，在 `-updateQuickAccessGeometryForHost:` 里**原地刷新**（同 centerY 的做法），字形视图边长与字形图片同步重算 —— 滑杆写完发 `ReloadPrefs` 即生效，不必 respring。**④ 关机键交互改造**：删除 `-powerHeld:`（原为长按 0.75s 弹系统关机面板），改为 `-powerTapped:` 单击弹出**圆形放射菜单** —— 以按钮为圆心、五项沿 78°→192° 弧线向左下放射展开，圆形 `UIVisualEffectView` 毛玻璃底 + 图标 + 两行文字，逐条错峰（0.028s）弹簧落位；背景遮罩与再点电源键均可收起。菜单项依次为 注销桌面 / 安全模式 / 用户空间重启 / 重新启动 / 关机；执行为 `posix_spawnp` 直接 exec（`sbreload` 优先、退回 `killall -9 SpringBoard`；安全模式 `killall -SEGV SpringBoard`；用户空间重启 `launchctl reboot userspace`；重启用 `launchctl reboot hard`，失败退回 `FBSSystemService -rebootWithOptions:`），不经过 shell，rootless 与 roothide 都能按 PATH 找到工具；关机保留系统面板（mobile 身份无断电权限）。**⑤ 快捷指令磁贴不显示定位**（日志显示添加本身成功：`catalog` 快捷指令 4→3、`op=add ... result=ok placed`、owned tile 4→5→6，但整条渲染链没有任何记录）：修三处 —— (a) `-updateOwnedDuplicateHostForOverlay:` 里快捷指令磁贴没有「源模块」却掉进「取第一个可见模块」的兜底分支，导致其 transform/opacity 被无关模块（如计时器）驱动而变透明，现改为不继承、恒为不透明；(b) 磁贴呈现层只在 `-applyRefinedLookToModule:` 画过一次性布局（那时 `bounds` 还是 0），现在在 `-layoutOwnedDuplicateModulesForOverlay:` 几何确定后补一次 `ERConfigureOddResizedModuleLayout`；(c) 新增 `SHORTCUTTILE` 状态诊断日志（每个标识符仅在状态串变化时记一行）。版本号 `control` / `Info.plist`（build 40→41）/ `Tweak.xm` 22 处 `ver=` 同步 1.0.1。 |
| 39 | 1.0.2 | **七个控制中心问题（依据用户 1.0.1 实机日志 `EchoReborn-log-20260916-112147.txt` 与截图，逐条处理）。** **① 快捷指令磁贴「加得进去、控制中心里却看不到」根治**：1.0.1 的三处修复不够 —— 1.0.1 的实机日志把链路走完了，磁贴已被正确摆到 75×75，却在**退出编辑模式**那一刻被压成 alpha 0：`SHORTCUTTILE … 75x75 proxy=0 viewAlpha=1.00` → `proxy=1 viewAlpha=0.00`。磁贴既没有原生模块视图、也没有「属于哪一页」的概念，任何按页／按选中态／按原生模块状态收敛可见性的规则都不该落在它身上。改为在快捷指令分支（`ERShortcutIdentifierIsShortcut`，`bringSubviewToFront:shortcutProxy` 之后）用 `performWithoutAnimation` 把 `module.view` 与 `oddPresentation` 每个布局回合都重新钉成可见（`hidden=NO` / `alpha=1` / `layer.opacity=1`），与 nowplaying 分支同一手法。 **② Sileo 图标完全本地化（核验）**：1.0.1 起 `control` 的 `Icon:` 已是 `file:///Library/PreferenceBundles/EchoRebornPrefs.bundle/icon.png`，本版实测 deb 载荷确含 `var/jb/Library/PreferenceBundles/EchoRebornPrefs.bundle/icon.png`（512×512 RGBA、0644、uid 501，rootless 由 Sileo 按越狱根解析、roothide 根即 `/Library/...`），全程不联网、无远程资源；设置入口图标 `EchoRebornPrefs.png/@2x/@3x` 不变。 **③ 关机面板改为「按类分组面板」（参考 EvoCenter16）**：`ERPowerMenuItemButton` 由放射菜单的圆形按钮改为**满宽横向行**（34pt 圆角色块 + 18pt SF Symbol + 16pt 动作名 + 右侧 chevron，高 52pt），新增 `kERPowerMenuPanelTag = 181063` 与 `kERPowerMenuItemSide = 52.0`，移除放射布局的 `kERPowerMenuTargetCenterKey`；`-presentPowerMenuFromButton:` 重写为一块 `SystemThickMaterialDark` 毛玻璃圆角面板（cornerRadius 30、白色 14% 描边）：`UIStackView` 竖排，顶部「电源」标题（20pt Bold）+ 关闭按钮，下面按用途分两组 —— **重启**（注销桌面／用户空间重启／重新启动）与**安全与电源**（安全模式／关机），每组一个 12.5pt 灰组标题；面板弹簧放大落位（0.44s／damping 0.82），条目逐条错峰（0.05+0.035i）上移淡入；`-dismissPowerMenuAnimated:` 改为整体淡出。执行路径不变（`posix_spawnp` 直接 exec `sbreload`／`killall`／`launchctl`，关机仍走系统 `SBUIPowerDownViewControllerFactory`）。 **④ 滑杆圆点缩小为原来的 2/3（四处统一）**：`prefs/ERSliderCell.m` 新增 `ERSliderThumbImage()`（`ERSystemSliderThumbDiameter = 26`、`ERSliderThumbScale = 2/3`、留 4pt 外边距容纳投影、`dispatch_once` 缓存），`ERSliderCell.h` 加 `FOUNDATION_EXPORT`；自绘 cell 的 `_erSlider` 在 `erSetupSubviews` 里 `setThumbImage:`（Normal + Highlighted）。**四个页面 12 个滑杆行全部走自绘 `ERSliderTrackCell`** —— `Glass.plist` 的「视差灵敏度」此前是唯一没写 `cellClass` 的原生滑杆行，本版补 `cellClass = ERSliderTrackCell` + `height = 54` + `erIcon = gyroscope`（teal），与其余 11 行外观完全一致。**弃用** 1.0.1 试做的 `ERPSSliderThumbInstaller`（swizzle 原生 `PSSliderCell -layoutSubviews`）：`prefs/ERUIHelpers.m` 删掉该分类与 `#import "ERSliderCell.h"`，文件尾留下「为什么不 swizzle」的说明 —— 一是 `PSSliderCell` 在运行期能否解析到本项目有相反记录（解析不到即死代码），二是它属 Preferences.framework，swizzle 会波及同一进程内其它插件的设置页滑杆。 **⑤ 滑杆行图标与文字对齐下方开关行**：实测 597px 宽截图／1.375 px-per-pt，开关行（`PSSwitchCell`）图标左缘 20.4pt、文字左缘 65.5pt，滑杆行原为 16pt／53pt（两列都偏左）。`ERSliderCell.m` 新增 `kERIconLeading = 20.0`／`kERIconSide = 29.0`（与 `ERUIHelpers.m` 的 `ERMakeIcon` size 一致）／`kERIconTextGap = 16.0`；`layoutSubviews` 改为**沿用框架算好的图标 frame**（`erIconView` 在 contentView 内找带图的 UIImageView），仅当 frame 不合理（宽高不在 20~40、`maxX` 越界）才退回固定几何，图标纵向居中；`titleX = maxX(icon) + 16`，于是 20+29+16 = 65pt 与开关行文字左缘对齐。标题列／滑杆／读数列的宽度算法不变（逐行一致）。 **⑥ 删除液态玻璃页「启用液态玻璃」下方的说明文字**：`Glass.plist` 开头那个 `PSGroupCell` 的 `footerText`（「从 LiquidAss 移植的液态玻璃渲染管线…总开关关闭时会实时移除已生效的玻璃效果。」）移除，保留空 `PSGroupCell` 维持卡片分隔。 **⑦ 日志页移除「当前日志」**：`Root.plist` 的「诊断日志」组删掉 `PSTitleValueCell`（label「当前日志」）那一行，只留「启用日志记录」「导出日志」「清空日志」；`ERRootListController` 的 `erRefreshLogStatus` 按 `erLogStatus` 标记属性查找 specifier，找不到即 return，删除后自然 no-op（代码保留）。 版本号：`control` 1.0.2（`Version:` + Description 追加 1.0.2 摘要）、`prefs/Resources/Info.plist` `CFBundleShortVersionString` 1.0.2／`CFBundleVersion` 41→42、`Tweak.xm` 全部 `ver=` 标记 1.0.1→1.0.2（25 处，含新增的 POWERMENU 诊断行）。 |
| 40 | 1.0.3 | **界面控件与模块管理交互六项（依据用户 `Preferences-2026-09-16-045830.txt` 崩溃报告、`EchoReborn-log-20260916-051128.txt` 日志与加减按钮参考图，逐项处理）。** **① 点击「关机」崩溃进安全模式（根治）**：两条路径叠加 —— (a) `erRebootViaFrontBoard`（Tweak.xm ~11320）把 **`nil` 当 `FBSShutdownOptions`** 传给 FrontBoard 的 `-rebootWithOptions:`，该调用在 SpringBoard 进程内，参数非法即抛异常 → ellekit 判崩 → 安全模式；(b) `erPerformPowerAction:` 的关机分支在 `erPresentNativePowerDownSheet` 返回 NO（面板没拉起来）时**静默回落**到上面那条重启路径，于是「点关机」等价于「触发一次非法重启」。修法：新增 `ERFBSShutdownOptions()`（~7645，先 `initWithReason:`、失败退回 `initWithReason:source:`，两者都不可用返回 `nil`），`erRebootViaFrontBoard` 取不到 options 即 `return NO` 不再调用 FBS；`erPerformPowerAction:` 关机路径改为 `BOOL presented = [self erPresentNativePowerDownSheet];`，`!presented` 时调 `erReportPowerDownUnavailable`（新增，~11303，弹「无法打开关机面板／请长按侧边按钮 + 任一音量键…」的 `UIAlertController`），**彻底删除重启兜底**；`erPresentNativePowerDownSheet`（~11231）改为同时尝试 `newPowerDownViewController` 与 `powerDownViewController` 两个选择器（各自 `@try` 保护）、presenter 改用新增的 `erModalPresenter`（~11270：枚举全部 `gOverlayControllers` + 键窗口根链，取第一个 `isViewLoaded && view.window && !presentedViewController && !isBeingDismissed` 的控制器，替换原先粗暴的 `gOverlayControllers.allObjects.firstObject`），整体包 `@try/@catch` 返回 NO；接口声明区（~7557）`erPresentNativePowerDownSheet` 返回类型 void→BOOL 并补 `erModalPresenter` / `erReportPowerDownUnavailable` 声明；`%hook SBUIPowerDownViewController`（~17474）的 `powerDownViewRequestCancel:` 与 `viewDidDisappear:` 补 `!self.isBeingDismissed` / `!self.isMovingFromParentViewController` 防止 dismiss 重入。 **② 快捷指令模块不显示（根因修复）**：磁贴图标由 `ERShortcutIconImage()` 从设置侧 `ERShortcutScanner` 发布到偏好键 `ShortcutCatalog` 的目录里按 UUID 取 `imageData`/`glyph`，而**目录 UUID 与磁贴记录的 UUID 大小写不一致时匹配落空** → `glyph` 被置空 → 磁贴无图标、等同透明空位。修法：`ERShortcutForIdentifier`（~2734）加大小写不敏感 UUID 兜底匹配；新增 `ERShortcutPlaceholderIcon()`（~2800，返回蓝色 sparkles `er.shortcut.placeholder`、`dispatch_once` 缓存）；快捷指令分支（~7028）改为 `UIImage *icon = ERShortcutIconImage(shortcut); if (!icon) icon = ERShortcutPlaceholderIcon();` —— **`glyph` 永不再有被置空的路径**，目录命中用真图、未命中用通用图。`SHORTCUTTILE` 日志（~7107）追加 `rec=%d icon=%d` 诊断。 **③ 滑轨两侧 − / + 步进按钮 + 双击键入**：`prefs/ERSliderCell.m` 新增 `kERStepButtonSide = 26.0`／`kERStepButtonGap = 5.0`，`erMakeStepButtonWithSymbol:action:`（26pt 圆、`tertiarySystemFillColor`、`cornerRadius=13`、SF Symbol minus/plus），`erStepSize`（整数区间→1.0，否则 0.1，对齐 `ERSpecFormatValue`），`erClampedValue:`／`erStepBy:`（吸附步长网格、写入、轻震动）与 `erStepDownTapped:`/`erStepUpTapped:`；双击读数 → `erValueLabelDoubleTapped:` → `erBeginValueEditing`（就地 `UITextField`、全选、首响应）→ `erCommitValueEditing`（`NSScanner` 解析 double、夹取、吸附，空/非数字取消）＋ `textFieldDidEndEditing:`/`textFieldShouldReturn:`；`_erValueLabel.textAlignment` 右对齐→**居中**；`erIconView` 跳过 `_erValueField`/`_erStepMinus`/`_erStepPlus` 以免误判为图标；`refreshCellContentsWithSpecifier:` 复用前先 resign 并隐藏输入框；`self.accessibilityValue = @"ER-slider-1003-stepbuttons"` 作静态校验锚点。**滑块与滑轨本身保持原样**，仅重排列宽：标题列 `kERTitleColumn 108→88`、读数列 `kERValueColumn 56→52`、`kERColumnGap 12→8`、`kERMinTrackWidth 80→70`。 **④ 滑杆读数与上方开关居中**：新增 `ERValueColumnCenter(width) = width − kERTrailing − kERSwitchWidth*0.5`（开关中心 = 宽度 − 41.5），读数列整列以该中心居中，所有带滑轨的控件一致。 **⑤ 模块管理子页文案**：`ERModuleSettingsController.m` 的 `self.title` 由「独立模块」→「**连接**」，分组 `groupSpecifierWithName` 由「独立模块」→「**模块管理**」（`ERRootListController.m` 的 `openModuleSettings:` 同步改 `controller.title = @"连接"`）；只改文案，行与功能不变。 **⑥ 7 个模块左图标**：`ERModuleList()` 每项加 `color` 键（橙/蓝/靛/绿/蓝/青/紫），`erSwitchForModule:` 给每个 specifier 挂 `erIcon`（模块 symbol）+ `erIconColor`，新增 `viewWillAppear:` 调 `erApplyIcons`、`viewDidLoad` 调 `erActivateIconRefresh`，复用 `ERUIHelpers` 的 29pt 彩色圆角芯片。 版本号：`control` 1.0.3（`Version:` + Description 追加 1.0.3 摘要）、`prefs/Resources/Info.plist` `CFBundleShortVersionString` 1.0.3／`CFBundleVersion` 42→43、`Tweak.xm` 全部 `ver=` 标记 1.0.2→1.0.3（25 处），校验脚本 `_verify_103.py` 同步（版本闸门 + 新增 `ER-slider-1003-stepbuttons`／`ERShortcutPlaceholderIcon`／`连接`／`模块管理`／`ERFBSShutdownOptions` 必检项）。 |
| 41 | 1.0.4 | **五项界面与发布问题（依据用户 `Preferences-2026-09-16-045830.txt` 崩溃报告与 `EchoReborn-log-20260916-051128.txt` 实机日志，逐项处理）。** **① 电源菜单按钮「第一次点击无响应、第二次点击进安全模式」**：面板以 `alpha = 0.0` 做 0.44s 淡入动画，但菜单行 `ERPowerMenuItemButton` 一加进视图就已是可点击状态，首次点击落在「透明但已响应」的行上被吞掉，第二次才真正命中（破坏性动作即表现为误触安全模式）。修法：`-presentPowerMenuFromButton:` 中动画前先 `[menu layoutIfNeeded]`，随后把所有行 `userInteractionEnabled = NO`；动画 completion 里才逐行恢复交互，并加 `if (![panel isDescendantOfView:host]) return;` 守卫防止面板已移除后回调仍改状态。 **② 快捷指令模块不显示（更名遗留状态未迁移）**：对照旧版 CCAster —— 更名同时改了偏好域（`com.futur3sn0w.ccaster.preferences` → `com.strive.echoreborn.preferences`）与磁贴标识前缀（`com.futur3sn0w.ccaster.shortcut.` → 新前缀），却没有迁移任何已落盘数据，于是更名前加进控制中心的老磁贴在新版里既认不出标识、也读不到 `COSMICDuplicateFamilies` / `ShortcutVisibleIdentifiers`。修法：新增 `kERLegacyShortcutIdentifierPrefix` / `kERLegacyPrefsDomain`，`ERShortcutIdentifierIsShortcut` 同时接受新旧两种前缀；新增 `ERShortcutNormalizedIdentifier()` 在写入/比较前把旧前缀改写为新前缀；新增一次性迁移 `ERMigrateRenamedPreferenceState()`，在 `%ctor` 中、`ERLoadPrefs()` 之前执行，把旧域的 `COSMICDuplicateFamilies` 与 `ShortcutVisibleIdentifiers` 整体搬到新域并改写其中的标识前缀。 **③ 设置页底部居中显示版本号**：新增页脚行（`ERVersionFooterText()` 运行期读取首选项 bundle 的 `CFBundleShortVersionString`，格式 `Echo Reborn <版本>`），12pt 灰色、`selectionStyleNone`；`ERRootListController -specifiers` 末尾追加一个 `groupSpecifierWithName:nil` 与一个静态文本 specifier（`cellClass` 指向运行期注册的 `ERVersionFooterCell`，与 `EREntryCell` 同一 `objc_allocateClassPair` + `+load` 手法）；新增 `scripts/er_set_version.py`，一条命令同步 `control` 的 `Version:`、`Info.plist` 的 `CFBundleShortVersionString`（并把 `CFBundleVersion` 单调 +1，本版 10004）与 `assets/depiction.json` 的版本文本，杜绝三处漂移（页脚此前长期显示旧号即因 depiction 与 Info.plist 不同步）。 **④ Sileo 图标不显示（jbroot 前缀缺失）**：`control` 的 `Icon:` 原为不带越狱根的包内相对路径 `file:///Library/PreferenceBundles/EchoRebornPrefs.bundle/icon.png`，rootless / roothide 下 bundle 实装于 `/var/jb`，Sileo 按此路径读不到文件（图标本身 512×512 RGBA，尺寸并非原因）。修法：改为 `file:///var/jb/Library/PreferenceBundles/EchoRebornPrefs.bundle/icon.png`。 **⑤ 模块管理子页文案回正 + 蓝牙/隔空投送图标**：导航栏标题由 1.0.3 的「连接」改回「**独立模块**」、页内顶部独立文案由「模块管理」改为「**连接**」；蓝牙与隔空投送只显示彩色底色没有字形，根因是 `bluetooth` / `airdrop` **并非 SF Symbol**（`UIImage systemImageNamed:` 返回 nil），真实字形位于 `/System/Library/ControlCenter/Bundles/ConnectivityModule.bundle`（隔空投送 `AirDropGlyph`、蓝牙为 CAPackage）。修法：`prefs/ERUIHelpers.m` 新增三级回落 `ERImageIconWithFallbacks(候选SF Symbol, 候选资源名, 兜底文字, 颜色)` —— SF Symbol → 系统 ConnectivityModule 资源目录 → 绘制兜底文字首字，`ERModuleList()` 为每个模块带上 `symbols` / `assets`，`erSwitchForModule:` 写入 `erIconSymbols` / `erIconAssets` / `erIconFallback` 供 cell 绘制。**另**：`Tweak.xm` 全部 `ver=1.0.3` 诊断串升为 `ver=1.0.4`；新增 `_tools/_verify_104.py` 作为本版 deb 内容校验器（两架构各 30 项全部 PASS）。 |
| 42 | 1.0.5 | **六项问题（依据用户 `Preferences-2026-09-16-045830.txt` 崩溃报告、`EchoReborn-log-20260916-051128.txt` 日志与电源菜单参考图，逐项处理）。** **① 快捷指令在控制中心不再显示（根因＝更名的状态迁移，不是合并）**：更名把偏好域（`com.futur3sn0w.ccaster.preferences` → `com.strive.echoreborn.preferences`）与磁贴标识前缀一起换掉，却**没有迁移已落盘数据** —— 插件对磁贴的所有权只记在老域的 `COSMICDuplicateFamilies` 里，域一换就读到空表，「哪些磁贴归我管」全部失效。1.0.4 虽加了一次性迁移，但写的是「新域已经有值就不动它」（`if (CFPreferencesCopyAppValue(cfKey, kERPrefsDomain)) continue;`），只要用户在新版里点过任一开关或重新添加过一次快捷指令，新域就已有值 → 整段搬迁被跳过 → 老配置永远搬不过来。修法：`ERMigrateRenamedPreferenceState` 改为**幂等的并集合并**（数组去重并集、字典逐键补齐且新域键优先，每次都跑，无「错过一次即永久丢失」窗口），纳入键从 2 个扩到 5 个（`COSMICDuplicateFamilies` / `ShortcutVisibleIdentifiers` / `ShortcutCatalog` / `ModuleGridOrigins` / `ModuleGridSizes`）；读取侧统一走新增的 `ERPreferenceValueWithLegacyFallback()`（新域无值回落 CCAster 老域，不改写老域、不破坏回滚）；新增 `ERNormalizeIdentifierKeys()` 在 `%ctor` 载入 `gERDuplicateFamilies` / `gERCustomOrigins` / `gERCustomSizes` 之后把仍是老前缀的**键**归一化到当前前缀（只动键不动值、幂等），避免新旧前缀各存一份导致同一磁贴在「添加控制项」里重复；`addShortcutModuleToControlCenter:` 不再硬要求 `ERShortcutForIdentifier` 非空（目录由设置侧扫描发布，用户没开过设置页时旧逻辑会连「添加」一起否掉），改为一律放行并记日志，绘制侧由 `ERShortcutPlaceholderIcon()` 兜底。**经排查，Soko / LiquidSiri 合并进单 dylib 与本次回归无关** —— 两子工程全树检索 `ControlCenter` / `CCUIModule` 零命中。 **② Sileo 图标仍不显示（路径必须跟随越狱方案）**：Sileo 把 `Icon:` 当 URL **原样**交给图片加载器，**不会**补越狱根（其 control 解析只把键名转小写后走 `URL(string:)`）。因此写死一个绝对路径不可能同时适配两套方案 —— rootless 落在 `/var/jb/Library/...`，roothide 落在 `<每台设备随机的 jbroot>/Library/...`；1.0.4 的 `file:///var/jb/...` 在 rootless 正确、在 roothide 必然失效（roothide 随机化 jbroot 正是为了让人写不死路径，其 Sileo 是 jbroot 感知的，所以 roothide 上正确的写法恰是**不带 jbroot 的 `/Library/...`**，与 roothide deb 里其它所有路径一致）。修法：`control` 里改回 jbroot 相对形式作源值，并在 `Makefile` 增加 `before-package::` 钩子按 `THEOS_PACKAGE_SCHEME` 改写暂存树里的 `DEBIAN/control`（此时该文件已由 deb.mk 生成）—— rootless 写 `/var/jb/Library/...`，roothide 写 `/Library/...`，并在构建日志打印最终生效值。经与用户确认**保持本地 `file://`、不引入任何网络请求**（未改用公开 https 直链）。图标本身为 512×512 RGBA 正方形 PNG，尺寸与格式本就满足要求。 **③ 电源菜单「点一下没反应、多点几下进安全模式」（重构 + 真根因）**：1.0.2~1.0.4 在把 `menu` 挂进宿主视图**之前**就 `activateConstraints` 了「`panel` ↔ `host.safeAreaLayoutGuide`」这批约束 —— 那一刻 `panel` 的祖先链止于 `menu`，而 `safeAreaLayoutGuide` 属于 `host`，两者**无共同祖先**，UIKit 当场抛 `NSInternalInconsistencyException`；异常发生在 SpringBoard 进程内，方法就地中断，面板根本不会出现（＝点一下没反应），反复点反复抛最终进安全模式。重构内容：**彻底不用 Auto Layout**（先 `[host addSubview:menu]` 再用 frame 摆面板与条目，不可能再出现跨层级约束）；版式对齐参考图 —— 浅色毛玻璃卡片（`UIBlurEffectStyleSystemMaterialLight`，跟随系统深浅色）、**五个满宽条目**（注销桌面 `arrow.clockwise.circle` / 安全模式 `shield.lefthalf.filled` / 用户空间重启 `exclamationmark.arrow.triangle.2.circlepath` / 重新启动 `arrow.clockwise.circle` / 关机 `power`）、文字左对齐、图标右对齐、行间通栏 1px 细分割线，去掉「电源」标题栏、关闭按钮、左侧彩色色块与 chevron；行高 56pt / 面板宽 340pt / 内边距 20pt / 图标 26pt / 文字 21pt Medium / 圆角 28pt（新增 `kERPowerMenu*` 常量与 `ERPowerMenuGlyph()` 候选符号链）；交互由「动画 completion 才解禁」（一旦动画被干扰就永久点不动，是「点了没反应」的第二条路径）改为**展示后 `kERPowerMenuTapGuard`＝0.28s 内丢弃一切点击**（条目、遮罩、电源键本身都算），既挡住「补的第二下」误触「安全模式」，又不会永久失效。 **④ 滑轨两侧 − / + 按钮加入震动反馈**：原实现用**局部** `UIImpactFeedbackGenerator`，方法一返回即释放，而 `impactOccurred` 是异步派发给 Taptic Engine 的 → 静默不震动且不报错。修法：`prefs/ERSliderCell.m` 新增全程强引用的静态生成器 `gERStepHapticGenerator` + `ERStepHapticFire()`（每次 `prepare`），仅在 `-erStepBy:` 里调用 —— **只服务这两个按钮**，滑块拖动 / 开关 / 双击输入都不经过该路径。 **⑤ 文案（仅改文字，图标与结构不动）**：「增强设置」→「**界面优化**」（`Root.plist` 入口行 + `Enhanced.plist` 页标题）；「音量控制」→「**音量百分比**」、「亮度控制」→「**亮度百分比**」。用户原话的「分别改为亮度/音量百分比」按字面会语义反转，已按「每一项显示自己的百分比」实现（保持"更直观"的原意）。 **另**：`Tweak.xm` 全部 `ver=1.0.4` 诊断串升为 `ver=1.0.5`（1.0.4 漏过一次），并把这一步纳入 `scripts/er_set_version.py`（现在一条命令同步 `control` / `Info.plist` / `depiction.json` / `Tweak.xm` 的 `ver=` **四处**）；新增 `_tools/_verify_105.py` 作为本版 deb 内容校验器。 |

| 43 | 1.0.6 | **快捷指令磁贴不显示（根因＝磁贴账本里的「空值坏账」）+ 可观测性补盲**（依据用户 1.0.5 实机日志 `EchoReborn-log-20260916-155308.txt`，并与 1.0.1 / 1.0.2 / 1.0.3 三份实机日志逐版对比）。 **① 根因：`COSMICDuplicateFamilies` 里存在「键正确、值为空串」的坏账条目。** 账本语义是 `identifier -> familyIdentifier`，快捷指令磁贴的 family 恒等于它自己。一条坏账同时造成三个后果：(a) `syncOwnedDuplicateModulesForOverlay:` 的 `if (!baseIdentifier.length) continue;` 直接跳过 —— 磁贴**根本不会被创建成子控制器**，它不是被藏起来而是没生成，所以屏幕上什么都没有，且 `SHORTCUTTILE` 诊断一行都不会为它打印；(b) 「添加控制项」用 `gERDuplicateFamilies[identifier]` 判「是否已添加」，空串是**非 nil 对象**故判真 —— 该快捷指令被永久隐藏，用户无法重新添加，即「怎么弄都不回来」；(c) 启动日志把账本条目数当成「已登记磁贴数」，报 10 个而实际创建 0 个，长期把排查带向错误环节。 证据链：1.0.5 日志中 `sync` 报 10 个 owned tile，目录报「4 shortcut(s) selectable … 56 hidden as already added」（设置页标记显示的 14 个减去可选的 4 个，正好 10 个被判「已添加」），两者数量吻合；而整份日志里这 10 个标识符**没有任何一条 `SHORTCUTTILE` 诊断**，只有新加的那 2 个有 —— 说明它们从未走到快捷指令分支，能同时满足「账本里有键」与「渲染时被跳过」的只有值为空这一种。修法：新增 `ERRepairDuplicateLedger()`，在 `%ctor` 读盘之后、任何使用之前把坏账就地补成合法值（快捷指令键的值必须是快捷指令标识符，否则一律补成键自身；保留 `#N` 编号标识符指向无后缀 family 的既有关系；非字符串键直接剔除；只补齐、不删除、幂等），并在 `syncOwnedDuplicateModulesForOverlay:` 开头每次再跑一遍；同函数内 `!baseIdentifier.length` 由「整条跳过」改为「按标识符自身兜底」。 **② 账本键与值一起归一化**：`ERNormalizeIdentifierKeys()` 原本只改键不改值，会留下「新前缀的键 → 老前缀的值」的半迁移状态（图标与布局按新标识符查得到，family 解析与一切按 family 的查表全部落空）；现在键与值同时过 `ERShortcutNormalizedIdentifier()`。 **③ 「已添加」判据收紧为「条目可用」**：新增 `ERDuplicateLedgerHasLiveEntry()`，目录的快捷指令候选与四处 `present` 集合都改为只认「值非空且为字符串」的条目 —— 坏账既渲染不出来，也不该把控制项从候选里挡掉，更不该让「添加」被静默拒绝（`present` 命中即 `return NO`）。 **④ 补掉可观测性盲区（本次排查耗时最长的原因）**：分页裁剪 `updatePagedModuleVisibilityForOverlay:` 写的是 `module.view.layer.opacity`，而 `SHORTCUTTILE` 诊断只读 `view.hidden` 与 `view.alpha` —— 盲区正好落在唯一还会让磁贴消失的那条路径上，于是日志一直报「一切正常」。新增 `ERVerifyShortcutTileVisibility()`（在 `layoutOwnedDuplicateModulesForOverlay:` 几何确定后、重画之前调用）：断言**正向不变量**「所在页 == 当前页 ⇒ 磁贴必须真的可见」（只把不可见的修成可见，绝不反向隐藏；页外裁剪仍完全交给 `updatePagedModuleVisibilityForOverlay:`；拖拽源不插手），并新增 `SHORTCUTVIS` 诊断行，记录 `page` / `cur` / `onPage` / `opacity` / `hidden` / `alpha` 与磁贴在屏幕上的实际中心。 **⑤ 日志不再谎报**：`sync: %lu owned tile(s) registered` 改为 `sync: ledger=%lu attached=%lu owned tile(s)`，同时报账本条目数与实际挂载数。 **⑥ 插件描述不再追加更新日志**：`control` 的 `Description` 由 1235 字的 1.0.0~1.0.5 累积更新日志改回 138 字的纯功能介绍（更新日志统一留在 `assets/depiction.json` 的「更新记录」分页），后续版本不再往包描述里追加。 版本号：`control` 1.0.6、`prefs/Resources/Info.plist` `CFBundleShortVersionString` 1.0.6／`CFBundleVersion` 10006、`assets/depiction.json` 版本文本与新增 1.0.6 更新记录条目、`Tweak.xm` 全部 `ver=` 标记 1.0.5→1.0.6（26 处）。 |

## 同步位置

改动版本号时**只需运行一条命令**（1.0.4 起）：

```
python scripts/er_set_version.py 1.0.6
```

它会同步下面**四处**，并把 `CFBundleVersion` 单调递增（不会再出现页脚 / depiction /
包版本 / 实机日志 `ver=` 四处漂移）：

1. `control` → `Version:`
2. `prefs/Resources/Info.plist` → `CFBundleShortVersionString` / `CFBundleVersion`
3. `assets/depiction.json` → 版本展示文本
4. `Tweak.xm` → 全部 `ver=X.Y.Z` 诊断串
